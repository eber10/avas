---
import Image from "astro/components/Image.astro"; 
const { title, person, image, width, height } = Astro.props; //Props que recibe la tarjeta (de index.astro)
const imgAlt = `Asesor de Inversión: ${person || 'Asesor Navas Inmobiliaria'}`; //Texto alternativo(alt) de la imagen
const gradientColor = 'rgba(20, 54, 99, 0.9)'; //Color azul oscuro para el degradado(similar al #143663)
---
<div class="bg-white rounded-xl shadow-xl overflow-hidden relative">
    <div class="w-full relative">
        <Image 
            src={image} 
            alt={imgAlt} 
            width={width}
            height={height}
            class="w-full h-full object-cover" 
        />
        <div 
            class="absolute inset-0 p-6 flex flex-col justify-end text-white" 
            style={`background: linear-gradient(to top, ${gradientColor}, rgba(20, 54, 99, 0.5), rgba(20, 54, 99, 0.1));`}
        >
        <h3 class="text-xl font-extrabold mb-1 uppercase leading-tight">{title || 'Beneficio Principal'}</h3>
            <a 
                href="#" 
                class="inline-block bg-[#F7931E] text-white text-xs font-bold text-center px-4 py-2 rounded-full w-fit hover:bg-orange-600 transition-colors my-2"
            >
                MÁS INFO AQUÍ
            </a>
            <p class="text-sm font-light mb-1">{person || 'Asesor de Ventas'}</p>
            <div class="flex justify-between items-center text-xs mt-1">
                <span class="font-bold">📞 983 624 649</span>
                <span class="font-light">@navasinmobiliaria</span>
            </div>
        </div>
    </div>
</div>